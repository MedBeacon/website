[build]
  publish = "."

# Security headers for all pages
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# No cache for HTML
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "no-cache"

# Long cache for images (1 year)
[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Short cache for CSS/JS (10 minutes)
[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=600"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=600"

# Branch deploy context
[context.branch-deploy]
  publish = "."

# ─────────────────────────────────────────────────────────────────────────────
# /demos/* — product demos built elsewhere and committed here as static output.
#
# /demos/desktop/ is the MedBeacon Desktop Prototype
# (repo: MedBeacon/MedBeaconDesktopPrototype), built with:
#     npm run build:site
# and copied here. It is a Phase-1 prototype: no backend, synthetic data,
# no PHI.
# ─────────────────────────────────────────────────────────────────────────────

# SPA fallback: the prototype is a client-side router, so a deep link like
# /demos/desktop/patient/12 has no file on disk and would 404. No `force`, so
# real files (the hashed assets) are still served directly and only unmatched
# paths fall through to the app shell.
[[redirects]]
  from = "/demos/desktop/*"
  to = "/demos/desktop/index.html"
  status = 200

# Content hashes in the filenames make these safe to cache forever. Overrides
# the 10-minute /*.css and /*.js rules above.
[[headers]]
  for = "/demos/desktop/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# The prototype makes no network calls of its own, so connect-src stays 'self'.
# The script-src hash covers the inline theme/zoom bootstrap in index.html
# (it must run before first paint to avoid a flash of the wrong theme, so it
# cannot be moved to an external file). Regenerate the hash if that inline
# script ever changes. style-src keeps 'unsafe-inline' for runtime inline styles.
[[headers]]
  for = "/demos/desktop/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'sha256-l3tWVz1kaxqFurFF3lgOl80Tptb/TvtctqcLozXfK88='; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"

# /demos/integrated/ is the MedBeacon Integrated MPage
# (repo: MedBeacon/MedBeaconIntegrated), built with `npm run build:demo`.
#
# The real app is a server: its Cerner registration is a confidential client,
# so the token exchange needs a secret and the FHIR reads go through a proxy.
# This build is not that. It swaps the API layer for one that reads Cerner's
# *open* R4 sandbox directly — the same synthetic patients, no authorization,
# no secret, no server. See that repo's public/js/api.static.js.

# Multi-page app, but the router is the filesystem, so this only catches
# mistyped paths and sends them to the one real page.
[[redirects]]
  from = "/demos/integrated/*"
  to = "/demos/integrated/index.html"
  status = 200

# Unlike the other demos this one does make a network call: the open Cerner
# sandbox. connect-src has to name that host or every FHIR read is blocked.
# No inline scripts in this build, so script-src needs no hash.
[[headers]]
  for = "/demos/integrated/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self' https://fhir-open.cerner.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"
